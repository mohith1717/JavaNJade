export function ErrorState({ title = "Unable to load data", message, onRetry }: { title?: string; message: string; onRetry?: () => void }) { return <div className="feedback-state error-state" role="alert"><span className="feedback-icon">!</span><div><strong>{title}</strong><p>{message}</p>{onRetry && <button className="secondary-button" onClick={onRetry}>Try again</button>}</div></div>; }
